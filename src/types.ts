export interface Section {
  id: string
  icon: string
  name: string
}

export interface MenuItem {
  id: string
  icon: string
  name: string
}

export interface CharacterAction {
  type: string
  id: number
}
